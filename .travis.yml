language: java

jdk:
- openjdk10
- openjdk11
- openjdk17
- oraclejdk18

addons:
  apt:
    packages:
    - libxml2-utils
  chrome: stable

before_install:
- source ./src/ci/before_install.sh

# skip the Travis-CI install phase because Maven handles that directly
install:
- 'true'

script:
- "./src/ci/build.sh"
